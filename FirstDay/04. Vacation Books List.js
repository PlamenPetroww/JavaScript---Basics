function hoursAll (input) {
    //⦁	Брой страници 
    const totalPages = Number(input[0]);
    //⦁	Страници
    const pagesPerHours = Number(input[1]);    
    //⦁Броят на дните
    const numOfDays = Number(input[2]);

    // SOLUTIONS OF THE PROBLEM

    const totalHours = totalPages / numOfDays;
    const finalHours = totalHours 
    console.log(hoursPerDay)
}

hoursAll(["212 ","20 ","2 "])