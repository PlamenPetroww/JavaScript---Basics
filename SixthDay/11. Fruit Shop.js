function fruitShop(input) {
    const banana = 2.5;
    const apple = 1.2;
    const orange = 0.85;
    const grapefruit = 1.45;
    const kiwi = 2.7;
    const pineapple = 5.5;
    const grapes = 3.85;

    const bananaWeekend = 2.7;
    const appleWeekend = 1.25;
    const orangeWeekend = 0.9;
    const grapefruitWeekend = 1.6;
    const kiwiWeekend = 3;
    const pineappleWeekend = 5.6;
    const grapesWeekend = 4.2;

    const fruit = input[0];
    const dayOfWeek = input[1];
    const kg = Number(input[2]);

    switch(dayOfWeek) {
        case "banana": 
    }
}

fruitShop(["apple","Tuesday","2"])